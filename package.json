{
  "name": "rhetorical-index-hybrid-instrument-alpha",
  "private": true,
  "version": "0.1.0",
  "description": "Dependency-light Instrument Alpha foundation beside the verified standalone Experience Prototype.",
  "scripts": {
    "typecheck": "tsc -p apps/web/tsconfig.json --noEmit",
    "build": "python3 tools/build_web.py",
    "test": "node --test tests/*.test.mjs && python3 -m unittest discover -s tests/python -p 'test_*.py'",
    "qa:runtime": "python3 tools/runtime_qa.py",
    "qa": "npm run typecheck && npm run build && npm test && npm run qa:runtime"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
