# Synthetic fixtures

The fixture bundle is intentionally fictional and uses `example.invalid` URLs. It exists to prove scanner, Compare, omission, evidence, and Event Record behavior without implying live reporting or fact checking.

Fixture findings are exact-span records. Tests require every finding excerpt to round-trip through its paragraph coordinates exactly.
